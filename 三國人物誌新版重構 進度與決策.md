# 三國人物誌新版重構｜進度與決策記錄

更新日期：2026-08-17
參與：宇軒（決策）、娜娜（Claude，本次對話）、奈奈（ChatGPT，歷史考據）
背景：舊版（`Three-Kingdoms-Biographies-Astro`）人物資料是巨大的巢狀 JSON，
`sourceId`／`personId`／`filterGroup` 等一堆代號互相參照，宇軒表示身為非
工程背景的維護者「不知道 ID 幹嘛的」「檔案太長」「不知從何下手」，因此
重構新版專案（GitHub repo `Three-Kingdoms-Biographies`）改用 Markdown +
YAML frontmatter 的人物資料格式。

## 零、重大流程轉折：從「打包 zip 整包覆蓋」改成「逐步從零重建」

**這是目前最重要、影響後續所有工作方式的決策，任何接手的 session 都要先
看這一段。**

原本的交付方式是：娜娜在沙盒裡改好整個 `src`／`public`，打包成 zip 交給
宇軒，宇軒解壓縮覆蓋到他本機用 `npm create astro` 建立的專案上。這個方式
在對話中造成實際問題（本機專案狀態跟沙盒不同步），宇軒要求改成「一步步
重新來過，每一步都知道自己在做什麼，不要再整包貼上覆蓋」，並選擇**真的
從零開始**：清空本機資料夾、重新 `npm create astro@latest`，每一個檔案
由娜娜逐一解釋用途、貼出完整內容、宇軒存檔確認沒問題後才進行下一步。

### 目前的標準作業流程（往後所有 session 都要照這個走）

1. 娜娜一次只交付一小塊（通常 1 個檔案，偶爾是「元件＋它依賴的 CSS」這種
   綁在一起才能動的最小單位），先用白話解釋這個檔案在畫面上負責什麼。
2. 貼出完整檔案內容（code block），註明要存到哪個路徑；如果只是小幅修改
   已交付過的檔案，改成給「找到這一段→改成這一段」的局部 diff，不整份重
   貼——但**宇軒可以隨時要求改回「直接給完整檔案貼上覆蓋」**（例如同一輪
   改動牽涉多處、diff 反而比整份還難對照時），這不是流程退步，兩種給法並
   存，依當下改動複雜度彈性選擇。
3. 請宇軒存檔後回報 `npm run dev` 有沒有紅字（很多檔案要等後面組裝頁面才
   會真正顯示出來，畫面沒變化屬正常現象）。
4. 宇軒確認沒問題才進下一步。
5. **絕對不要**在這個重建過程中重新打包整個 `src`／`public` zip 交給宇
   軒——那是舊流程，已經被明確放棄。
6. 娜娜這邊沙盒裡留一份 `/home/claude/work/repo` 作為「已驗證版本」的本地
   對照 repo，每次交付完都要記得把改動實際套用回這份沙盒 repo（不能只在
   聊天訊息裡描述改動），並在下一次要交付新檔案前用 `npx astro check` 驗
   證整個工程 0 errors，避免沙盒對照版跟聊天室裡講過的內容兜不起來。

### 已知的複製貼上陷阱：`<a` 開頭標籤會在複製時整行消失

交付 `SiteHeader.astro` 時發生過、後來交付 `HomeHero.astro` 時又發生一
次：宇軒從對話介面複製 code block 貼到 VS Code 後，「`<a` 單獨佔一行、
屬性另起多行」的寫法，開頭的 `<a` 那一行會完整消失，只剩屬性跟後面的
`</a>`，導致標籤沒配對，VS Code／Astro 語言伺服器丟出一整串看起來很嚇
人、彼此無關的語法錯誤（`ts2364`／`ts1005`／`ts1382`／`ts1381`／「找不到
名稱 'faction'，您指的是 'factions' 嗎？」這種）。**因應方式**：之後如果
又出現一整串彼此無關、位置集中在同一個標籤附近的語法錯誤，優先懷疑是不
是某個開頭標籤在複製貼上時消失了；建議複製 code block 時用區塊右上角內
建的「複製」按鈕，不要手動反白選取。**規避寫法（已全面採用）**：
- 標籤可能是 `<a>` 或別的標籤（例如「對方有沒有建檔」這種條件式連結）：
  一律用一個大寫變數（`TargetTag` / `LeaderTag` = `target ? "a" : "div"`）
  動態決定要渲染什麼標籤。
- 無條件一定是 `<a>` 的情況：把 `<a` 跟第一個屬性合併寫在同一行，例如
  `<a class="faction-card faction-card--${faction.id}"`，不要讓 `<a`
  自己單獨佔一行。
- 這兩種寫法已經用在 `PersonOverview.astro`／`PersonRelations.astro`／
  `[faction].astro`／`HomeHero.astro`／`[id].astro`／`PersonHero.astro`。

### 已知的踩坑模式：`getCollection()` 回傳型別推斷不出來，連鎖噴一整排錯

`[faction].astro`、`[id].astro` 都發生過：`getCollection("people")` 拿到
的資料在後續 `.map()`／`.filter()`／`.sort()` callback 裡型別被推成
`unknown`／`never`，導致參數被標成 implicit-any（`ts7006`），
`frontmatter.xxx` 被標成「never 型別沒有這個屬性」（`ts2339`），甚至把
`Map<unknown, unknown>` 傳給元件 props 時型別對不上（`ts2322`）。**修法固
定套路**：
1. 從 `astro:content` import 時多 import `type CollectionEntry`。
2. 需要用到的地方（`getStaticPaths` 參數、`interface Props` 裡的欄位、
   `.map()`／`.filter()` callback 參數）都明確標成 `CollectionEntry<"people">`
   或 `CollectionEntry<"people">[]`。
3. 頁面組件裡用 `Astro.props as Props` 取值。
4. 別漏掉巢狀 callback（例如 citations 陣列裡的 `(sourceLine: string) => {}`）
   ——這個最容易漏標，會在前面幾個都修完後才單獨冒出來。

### 已知的踩坑模式：改了 content collection 的 schema，畫面卻還是舊行為

把 `summary` 欄位從單一字串改成陣列（見第七節）之後，宇軒本機一度出現
`identity.summary.map is not a function`，`content.config.ts` 確認已經
改成新版、`npm run dev` 也重開過，錯誤依然存在。**原因**：content
collection 的資料有自己一層獨立快取，存在專案資料夾裡一個叫 `.astro` 的
隱藏資料夾，單純重開 dev server 有時候不會強制重新驗證每一份 `.md`（尤
其是「資料本身沒改、只有 schema／transform 改了」的情況）。**解法**：把
`npm run dev` 停掉，刪除專案資料夾裡的 `.astro` 隱藏資料夾（純快取，不會
動到自己寫的任何內容），再重新 `npm run dev`，會自動重建並吃到新
schema。之後只要是「改了 `content.config.ts` 的欄位型別或 transform，
但畫面行為看起來還是照舊」，都先懷疑是這個快取沒刷新，直接請宇軒刪
`.astro` 資料夾重開，不用一直檢查程式碼有沒有貼對。

## 一、人物頁資料格式（已定案，維持不變）

- 首頁：人物故事＋詳細資料為主、帶一點遊戲風格、勢力分類必留。
- 人物內容欄位：頭像、姓名、字號、簡介、籍貫、生卒年、諡號、所屬勢力與
  效力歷程、官職、親屬關係〔史實／後世分層〕、評價〔當世／後世〕、生平
  〔史實／演義分層〕、著作。
- **人物資料改用 Markdown + YAML frontmatter**，取代原本的 TypeScript 物
  件（`src/data/people/*.ts`）。最上面「填空區」放一次填好的基本資料，底
  下用固定的中文標題（`##`／`###`）＋段落／條列寫內容，史料出處直接寫書
  名、用「（來源：……）」註記，人物之間的連結用「（連結：目標人物代
  號）」，對方未建檔就整段不寫、之後補上會自動生效。
- `src/lib/parsePersonBody.ts`：不使用 Astro 內建的 Markdown 轉譯結果，直
  接讀 `entry.body` 原始文字自己解析固定中文標題／條列語法。

## 二、親屬關係卡片 UI 設計（已定案並實作完成）

- 一筆親屬記錄的畫面分三排：
  1. **人物名稱一排**：頭像＋姓名，對方已建檔就是可點連結＋真頭像，未建
     檔則不能點、頭像改用佔位圖（見「佔位頭像」）。名稱獨立一排，避免長
     名稱（例如「夏侯氏（名稱不詳）」）把下面的標籤擠壓變形。
  2. **標籤一排，左右分開放**：左邊是親屬分類標籤（父母／兄弟姊妹／配
     偶／子女／孫輩／其他，來自 `relationGroupLabel`，不管切史實還是後世
     都不會變）；右邊是資料性質標籤，只有哪一層有內容才顯示對應標籤。
  3. **內文一排**：顯示目前作用中那一層的說明文字。
- **右邊標籤定案為「史實」／「後世」（不是「演義」）**：現代作品併入後
  世文學，不再獨立拆出第三顆標籤；非史實那一層的說法不一定來自《三國演
  義》本身（可能是後世小說、遊戲等改編），標籤寫死「演義」會誤導讀者。
- **兩層都有內容時**：兩顆標籤都顯示、可點擊切換，預設先顯示「史實」；
  只有一層有內容時，該標籤顯示成不能點的純標示。
- **卡片固定高度＋內部捲動（本輪定案，取代原本的 `align-items: start`
  方案）**：`.person-relation-card` 改成 `display:flex; flex-direction:
  column; height: 280px;`，`.person-relation-card__content`（史實／後世
  內文）改成 `flex:1; overflow-y:auto; overflow-x:hidden;`，切換史實／
  後世時卡片本身跟同一列其他卡片都不會跟著變高變矮，內文太長就在卡片內
  部往下捲動。捲軸樣式拿掉白色軌道，只留一條細細的、跟卡片同色系（
  `--person-bronze`）的捲軸；另外加了 `overscroll-behavior: contain`，
  捲到內文頂／底之後滑鼠滾輪不會繼續帶動整個人物頁面。`280px` 是抓「大概
  完整顯示 3～4 行內文」的經驗值，之後填了劉備、關羽這種內文較長的關係，
  實際看效果再調整。
- **實作方式**：`PersonRelations.astro` 用 `data-relation-card` /
  `data-relation-layer-btn` / `data-relation-layer` 這組 data attribute，
  搭配檔案底部一段 `<script>` 做點擊切換（跟 `[id].astro` 主頁籤、
  `[faction].astro` 搜尋框同一套「data attribute + 原生 JS」模式）。
- **已知簡化、之後可能要擴充**：「後世」標籤底下如果同時有演義、現代遊戲
  等不同來源，目前 `ParsedRelation.romance` 還是單一字串欄位，這種子分類
  目前**由撰稿人自己在同一段文字裡手動寫清楚**，解析器沒有幫忙拆成結構化
  欄位。等實際填寫劉備／關羽這類真的有多重後世來源的人物、發現手動格式
  不夠用時，再回頭擴充 `parseRelations()`。

### 佔位頭像（已定案並實作完成）

宇軒提供一份 `_placeholder.svg`（沿用舊專案素材），**取代掉原本「姓氏第
一個字＋圓圈」的文字佔位做法**，套用在所有「對方沒有自己人物頁」的頭像位
置——親屬卡片、效力歷程的君主／效力對象都算。存到
`public/images/_placeholder.svg`，沒有真頭像時 `src` 就指向
`${base}images/_placeholder.svg`。**待確認**：宇軒尚未回報是否已實際把
`_placeholder.svg` 放進 `public/images/` 資料夾。

### 史實／演義視覺色彩區分（已定案並實作完成）

- **時間軸連接槓槓一律顯示**（拿掉最後一筆隱藏連接線的規則）。
- **新增顏色變數 `--person-romance: #3f6e63`**（低彩度墨綠），跟既有
  `--person-cinnabar`（朱紅，史實／引用色）配對成「史實＝紅、後世＝綠」
  的顏色邏輯，選這個墨綠色是為了跟蜀漢勢力既有的橄欖綠
  `--person-tone: #68745c` 區隔開。
- 套用在生平時間軸的圓點（連接線本身維持中性色不變）與親屬卡片史實／後
  世標籤（idle 淡色調／`.is-active` 實色填滿）。
- **刻意不變色的例外**：生平段落裡「與史實的差異」提示框維持朱紅色，不
  跟著演義的墨綠色走——娜娜的設計判斷（提醒讀者注意落差的警示色，跟「這
  段內容屬於史實」的分類色語意不同），宇軒尚未明確表態是否認同，可再
  調整。

## 三、人物卷首簡介（`summary`）：真正的分段模型

宇軒實測發現舊做法（`summary: >` 折疊語法 + 一律砍掉所有換行的 transform）
造成兩個問題：手動斷行方便閱讀時，斷行處會多一個不該有的空格（因為 YAML
折疊語法把換行變成空格）；後來改成砍光所有換行，又變成「真的想分段」也
沒用。進一步釐清宇軒真正要的其實是**單純換行（像按 Enter 換一行接著
寫），不是段落之間隔一大塊空白**。

**最終定案：**
- `content.config.ts` 的 `summary` schema：
  ```ts
  summary: z
    .string()
    .transform((value) =>
      value
        .split(/\n{2,}/)
        .map((paragraph) => paragraph.replace(/\r?\n\s*/g, "").trim())
        .filter(Boolean),
    ),
  ```
  一定要用 `summary: |`（literal block，保留原始換行），不要用 `>`
  （folded block，換行會被 YAML 自己吃成空格，源頭就跟這裡想要的行為衝
  突）。
- `.md` 裡的規則：純粹手動斷行方便編輯（沒有空白行）＝接成同一段，斷行
  處不留空格；真的想換行分隔＝空一整行。
- `PersonHero.astro` 渲染時，**這個陣列不是渲染成多個 `<p>`（那樣段落
  間會有明顯的大留白，宇軒不要這種效果）**，而是渲染成同一個
  `<p class="person-hero__summary">`，段落之間用 `<br />` 銜接：
  ```astro
  <p class="person-hero__summary">
    {identity.summary.map((paragraph, index) => (
      <>
        {index > 0 && <br />}
        {paragraph}
      </>
    ))}
  </p>
  ```
- `src/types/person.ts` 的 `PersonIdentity.summary` 型別對應改成
  `string[]`。
- 踩坑記錄：改完這組 schema 之後宇軒本機一度沒生效，是 content
  collection 快取的問題，解法見第零節「已知的踩坑模式」最後一條（刪
  `.astro` 資料夾）。

## 四、人物總覽／生平段落的史料出處：抽成獨立小字清單

宇軒要求「生平概覽」（人物總覽卷首那段介紹）跟生平段落結尾的
「（來源：……）」不要混在內文裡，字要比正文小一點，格式要跟「官職與爵
位」那種乾淨的書名＋卷次清單一致，為之後串連三國文獻庫連結做準備。

**做法**：`parsePersonBody.ts` 的段落解析（`groupIntoParagraphBlocks` 用
於人物總覽、`parseBiographySection` 的一般段落用於生平）現在都會把段落
最後一行「（來源：……）」抓出來變成獨立的 `citations: ParsedCitation[]`
欄位，不再留在段落文字裡。新增 `ParsedParagraph { text, citations }`
型別取代原本單純的 `string[]`。畫面上直接沿用「官職與爵位」已經在用的
`.person-rank-list__sources` 樣式（沒有另外造一組新樣式），書名／卷次
分開顯示；CSS 裡原本就有一組沒被用到的 `.person-overview__citations`
相關樣式（連結 hover 變色都寫好了），推測是更早期就預留給這個功能、只是
解析器一直沒跟上，這次直接沿用既有 class 名稱風格。

**影響範圍**：`ParsedPersonBody.introParagraphs`、
`ParsedBiographyEntry.paragraphs` 型別都從 `string[]` 改成
`ParsedParagraph[]`；`PersonOverview.astro`、`PersonBiography.astro` 的
段落渲染要跟著改（`{paragraph.text}` + 有 citations 才渲染小清單）。
**這是為文獻庫連結鋪路**：之後要把來源文字包成可點擊連結，直接改
`.person-rank-list__sources` 的 `<li>` 渲染邏輯（把純文字換成 `<a>`）
即可，不用再動資料結構。

**注意事項（撰稿規則變更）**：以後在人物總覽／生平段落結尾附史料出處，
「（來源：……）」**必須自己獨立一行**，不能跟前一句黏在同一行，解析器
才抓得到；官職與爵位、親屬關係原本就是這樣寫的，現在人物總覽跟生平也統
一成這個規則。

## 五、首頁（`index.astro` ／ `HomeHero.astro`）

沙盒裡本來就留著一版更早期做的首頁草稿（水墨背景、勢力入口卡片、右側
無名說書客），只是搜尋名單跟各勢力人數都是暫時寫死的假資料，這次接回
真實資料：

- `index.astro`：搜尋名單改成直接讀 `getCollection("people")`，之後每加
  一個人物 `.md`，搜尋自動就會多一筆。
- `HomeHero.astro`：勢力入口卡片「已收錄 X 人」改成即時統計
  `content/people` 底下各勢力筆數，不再是寫死的數字。
- `home-hero.css` 本身沒有改動，圖片素材（水墨背景、五個勢力代表人物立
  繪、說書客立繪、favicon）沙盒裡都已經有現成的，不像張飛／關平頭像那樣
  缺檔。
- 首頁配色維持原樣（最暗、最有氛圍那一層，不用跟著勢力總覽的調亮一起
  動）。

## 六、勢力總覽配色（本輪重新設計，已定案並實作完成）

宇軒觀察到人物詳細頁（亮色宣紙底）跟勢力總覽（原本沿用舊站的暗色底）反
差太大，兩個頁面點過去感覺像換了網站；討論後決定的方向是**調亮勢力總
覽去貼近人物詳細頁，而不是把人物詳細頁壓暗**——理由是人物詳細頁是使用
者「抵達的目的地」，值得比列表頁更亮、更聚焦，犧牲掉這個「抵達感」不划
算；勢力總覽是相對沒有經過這輪重新設計的舊配色，改起來範圍集中。

**具體改動（`src/styles/faction-overview.css`）**：
- 整頁底色從近黑色（`color-mix(勢力色 26%, black)`）改成跟人物詳細頁
  `--person-paper` 同一種算法的亮色宣紙（`color-mix(勢力色 18%, #d3c9b7)`）。
- 搜尋框、清除按鈕、無搜尋結果的背景從深色半透明改成米白色半透明；清除
  搜尋按鈕圖示拿掉了原本為暗底做的反色濾鏡。
- 人物竹簡卡片：拿掉原本「往右逐漸退成幾乎沒有顏色」的三段漸層，改成統
  一、看得出來的淡勢力色（`color-mix(勢力色 24%, #f2e9d6)`）；左側色線
  （`.faction-person-slip__accent`）維持較飽和的勢力色（opacity 從 0.7
  提高到 0.9），跟卡片本身的淡色形成「同色系、深淺分層」；頭像佔位背景
  從近黑色改成暖米色；人物姓名文字原本寫死淺色 `#f3ede1`，改成跟著頁面
  走的深墨色變數。
- 陰影／邊框方向反過來（原本「暗底打白色高光」改成「亮底打深色陰影」），
  效果一樣是立體感。
- **勢力橫幅維持深色**，跟人物詳細頁卷首（`.person-hero`）用同一種設計
  邏輯：整頁是亮的，最上面那塊「招牌」用深色勢力主題色壓陣、配白字。第
  一版橫幅漸層公式（`color-mix(勢力色 88%, 近黑)` 之後迅速壓成純黑）被
  宇軒回報「太暗」，改成跟 `.person-hero` 完全一樣的三段漸層公式（`深→
  中→淺`），新增 `--faction-color-dark` / `--faction-color-light` 兩個
  變數、數值直接對應人物詳細頁 `--person-tone-dark` / `--person-tone-
  light` 同一個勢力，取代原本的 `--faction-color-soft`（已不再使用，移
  除）。兩個頁面同一勢力的橫幅／卷首顏色現在完全一致。

## 七、人物詳細頁「回到＿＿人物總覽」

第一版做成獨立的麵包屑列放在卷首（`.person-hero`）上方、亮底配深字，宇
軒回報「含在橫幅內比較不突兀」，改成放進 `.person-hero__inner` 網格內、
橫幅深色底配淺色字，視覺上跟「首頁 ／ [人物姓名]」融為一體：
- `PersonHero.astro` 自己算麵包屑（它本來就已經用 `getFactionById` 算勢
  力資料給背景大字用，直接沿用同一份 `faction` 變數），不再需要
  `[id].astro` 額外傳 props 或另外渲染一段獨立的 `<nav>`。
- `.person-hero__inner` 的 `gap: 54px` 拆成 `column-gap: 54px` /
  `row-gap: 8px`，讓新增的麵包屑列跟下面的頭像／資料列之間不會被原本給
  頭像跟文字用的大間距撐開。
- 對方沒有勢力資料時（理論上不會發生，五個勢力都有資料）退回顯示人物
  名稱純文字，不當連結。

## 八、GitHub Actions 自動部署

宇軒希望 GitHub Desktop commit＋push 之後自動 build／deploy。沙盒 clone
下來的舊倉庫紀錄裡其實已經有一份現成、應該是先前就測過可用的
`.github/workflows/deploy.yml`（`actions/checkout` → `withastro/action`
build → `actions/deploy-pages` 部署），推測是「從零重建」清空本機資料夾
時沒有一起搬過去，並不是要重新設計。已經把完整內容交給宇軒，請他自己在
本機專案建立 `.github/workflows/deploy.yml`。**還有一個只有宇軒能確認的
設定**：GitHub 網頁上 repo → Settings → Pages → Build and deployment →
Source 要是「GitHub Actions」（不是「Deploy from a branch」），這個開關
娜娜這邊沒有權限查看目前狀態。

## 九、目前「從零重建」進度（Task List 狀態，供接手者對照）

1. ✅ 清空本機專案資料夾、重新 `npm create astro@latest`。
2. ✅ `astro.config.mjs`（含 `markdown: { processor: unified() }`）。
3. ✅ `src/content.config.ts`（`people` content collection + zod schema，
   `summary` 已改成段落陣列 transform，見第三節）。
4. ✅ `src/lib/parsePersonBody.ts`（含第四節的來源抽取邏輯）。
5. ✅ 人物詳細頁元件與樣式全部交付完成，含本輪所有調整（親屬卡片固定高
   度＋捲動、卷首含麵包屑、`summary` 換行邏輯、來源小字清單）。
6. ✅ `src/pages/character/[id].astro`，`guan-yu.md` ＋ `guan_yu.png`，第
   一個人物頁（關羽）確認畫面正常。
7. ✅ `src/pages/faction/[faction].astro` ＋ `src/styles/
   faction-overview.css`（本輪已改成亮色主題，見第六節），`zhang-fei.md`
   ／`guan-ping.md` 已放回，但頭像圖檔 `zhang_fei.png`／`guan_ping.png`
   仍缺，需宇軒自己提供。
8. ✅ `src/pages/index.astro` ＋ `src/components/HomeHero.astro`（見第五
   節），宇軒未回報是否已測試確認畫面正常。
9. ⏳ 下一步：`liu-bei.md`（劉備人物資料）——尚未開始撰寫；`.github/
   workflows/deploy.yml`——已交付內容，待宇軒自己建檔＋確認 GitHub Pages
   Source 設定＋實際 push 測試一次。

## 十、重要踩坑記錄：Astro 7 的 Sätteri Markdown 引擎在 Windows 上裝不上

宇軒在公司 Windows 電腦（不能裝額外系統元件、不能動 Node 版本）上跑
`npm run dev` 時，卡在 `[content] Syncing content` 這一步，報錯是找不到
`@bruits/satteri-win32-x64-msvc` 的原生 `.node` 檔案。

**根本原因：** Astro 6.4 引進、Astro 7 起預設使用 Sätteri（Rust 寫的原生
Markdown 處理器）處理 content collections；舊版人物資料是純 TypeScript 物
件，從來沒有觸發過任何 Markdown 處理流程，這次改用 `.md` 檔案才第一次真
正呼叫到這顆原生引擎。

**解法（已驗證有效，已內建在「從零重建」流程第 2 步）：** 在
`astro.config.mjs` 加上 `markdown: { processor: unified() }`（`import
{ unified } from "@astrojs/markdown-remark"`），並安裝
`@astrojs/markdown-remark`，換回純 JavaScript、沒有原生依賴的
Unified／Remark 處理器。人物頁是自己讀 `entry.body` 原始文字解析，不吃
Astro 內建轉譯結果，換引擎對畫面顯示零影響。

## 十一、內容落差盤點（對照舊版 Three-Kingdoms-Biographies-Astro）

- **必須補回、待宇軒／奈奈確認設計後才動工**：三國文獻庫（獨立網站）的引
  用連結功能——舊版靠 `sourceId` → `sources.json` 對到 `text-units.json`
  產生可點擊連結。新版目前引用只是純文字，第四節已經把來源抽成結構化
  `citations`，是這個功能的前置工作，但連結機制本身（比對文字 or 走
  ID）還沒定案。**娜娜提出的方案（尚未實作，等確認）**：新增
  `src/data/referenceLinks.ts` 存放 `{ source, locator?, url }` 清單，畫
  面渲染時自動比對「（來源：書名・卷次）」文字，比對到就包成連結，比對
  不到維持純文字。
- **可以放心捨棄**：`published`／`draft` 發布旗標（改用 git commit／push
  當作發布關卡）、`filterGroup`（新版 faction + factionStages 已涵蓋）。
- **待宇軒自己判斷**：`otherNames` 要不要比照舊版拆成 `otherNames`／
  `commonAlias`（後世通稱）兩種，涉及既有的「保守命名原則」，還沒決定。

## 十二、待辦／下一步

- 等宇軒確認本輪交付的所有檔案（親屬卡片固定高度＋捲動、勢力總覽亮色主
  題、橫幅顏色、竹簡淡綠底、人物頁麵包屑含在橫幅內、`summary` 換行邏
  輯、生平來源小字清單）貼上後畫面正常。
- 宇軒需自行提供：`zhang_fei.png`、`guan_ping.png` 頭像圖檔；確認
  `_placeholder.svg` 是否已放進 `public/images/`；`.github/workflows/
  deploy.yml` 自己建檔並確認 GitHub Pages Source 設定。
- 下一步：`liu-bei.md`（劉備資料）尚未開始撰寫。
- 文獻庫引用連結機制：等宇軒確認第十一節方案後動工，第四節的 citations
  結構化已經是前置準備。
- 程式碼交付方式已全面改成逐步貼檔案內容確認，**不再打包 zip 整包覆
  蓋**，視改動複雜度可彈性改用「整份檔案貼上覆蓋」取代局部 diff。沙盒
  clone 僅本機 commit、未 push（宇軒未提供 push 權限），宇軒之後要正式
  上 GitHub 需自己手動 push（習慣用 GitHub Desktop）。
- 其餘人物（劉備、張飛、關平等）依新格式補檔；張飛／關平內容已放回但缺
  頭像圖檔，劉備尚未撰寫。
- `parseRelations()` 的「後世」子分類（演義／現代遊戲…）目前靠撰稿人手
  動格式化，還沒有結構化欄位，等實際使用發現不夠用再擴充（見第二節）。
- `parsePersonBody.ts` 只驗證過關羽＋張飛／關平幾份資料的寫法，還沒有大
  量不同人物、不同邊界情況的壓力測試。
- 「與史實的差異」提示框維持朱紅、不跟著演義綠色走——娜娜的設計判斷，宇
  軒尚未明確表態是否認同，可再調整（見第二節）。
- 「網頁元件怎麼拆」這個討論尚未細談。
- `otherNames`／`commonAlias` 拆分決策待宇軒定案。
- 舊版 Astro 專案（`Three-Kingdoms-Biographies-Astro`）與文獻庫
  （`Three-Kingdoms-Reference-Texts`）維持獨立不動，不併進來討論。