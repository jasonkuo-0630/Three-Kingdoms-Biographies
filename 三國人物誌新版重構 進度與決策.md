# 三國人物誌新版重構｜進度與決策記錄

更新日期：2026-08-17
參與：宇軒（決策）、娜娜（Claude，本次對話）、奈奈（ChatGPT，歷史考據）
背景：舊版（`Three-Kingdoms-Biographies-Astro`）人物資料是巨大的巢狀 JSON，
`sourceId`／`personId`／`filterGroup` 等一堆代號互相參照，宇軒表示身為非
工程背景的維護者「不知道 ID 幹嘛的」「檔案太長」「不知從何下手」，因此
重構新版專案（GitHub repo `Three-Kingdoms-Biographies`）改用 Markdown +
YAML frontmatter 的人物資料格式。

## 零、重大流程轉折：從「打包 zip 整包覆蓋」改成「逐步從零重建」

**這是目前最重要、影響後續所有工作方式的決策，任何接手的 session 都要先
看這一段。**

原本的交付方式是：娜娜在沙盒裡改好整個 `src`／`public`，打包成 zip 交給
宇軒，宇軒解壓縮覆蓋到他本機用 `npm create astro` 建立的專案上。這個方式
在對話中造成實際問題（本機專案狀態跟沙盒不同步，`[faction].astro` 冒出
implicit-any 錯誤），宇軒要求改成「一步步重新來過，每一步都知道自己在做
什麼，不要再整包貼上覆蓋」，並選擇**真的從零開始**：清空本機資料夾、重
新 `npm create astro@latest`，每一個檔案由娜娜逐一解釋用途、貼出完整內
容、宇軒存檔確認沒問題後才進行下一步。

### 目前的標準作業流程（往後所有 session 都要照這個走）

1. 娜娜一次只交付一小塊（通常 1 個檔案，偶爾是「元件＋它依賴的 CSS」這種
   綁在一起才能動的最小單位），先用白話解釋這個檔案在畫面上負責什麼。
2. 貼出完整檔案內容（code block），註明要存到哪個路徑；如果只是小幅修改
   已交付過的檔案，改成給「找到這一段→改成這一段」的局部 diff，不整份重
   貼——但**宇軒可以隨時要求改回「直接給完整檔案貼上覆蓋」**（例如同一輪
   改動牽涉多處、diff 反而比整份還難對照時），這不是流程退步，兩種給法並
   存，依當下改動複雜度彈性選擇。
3. 請宇軒存檔後回報 `npm run dev` 有沒有紅字（很多檔案要等後面組裝頁面才
   會真正顯示出來，畫面沒變化屬正常現象）。
4. 宇軒確認沒問題才進下一步。
5. **絕對不要**在這個重建過程中重新打包整個 `src`／`public` zip 交給宇
   軒——那是舊流程，已經被明確放棄。
6. 娜娜這邊沙盒裡留一份 `/home/claude/work/repo` 作為「已驗證版本」的本地
   對照 repo，每次交付完都要記得把改動實際套用回這份沙盒 repo（不能只在
   聊天訊息裡描述改動），並在下一次要交付新檔案前用 `npx astro check` 驗
   證整個工程 0 errors，避免沙盒對照版跟聊天室裡講過的內容兜不起來。

### 已知的複製貼上陷阱：`<a` 開頭標籤會在複製時整行消失

交付 `SiteHeader.astro` 時發生過：宇軒從對話介面複製 code block 貼到 VS
Code 後，兩處「`<a` 單獨佔一行、屬性另起多行」的寫法，開頭的 `<a` 那一行
完整消失，只剩屬性跟後面的 `</a>`，導致標籤沒配對，VS Code／Astro 語言伺
服器丟出一整串看起來很嚇人、彼此無關的語法錯誤（`ts2364`／`ts1005`／
`ts1382`／`ts1381`）。**因應方式**：之後如果又出現一整串彼此無關、位置集
中在同一個標籤附近的語法錯誤，優先懷疑是不是某個開頭標籤（尤其是像
`<a`、`<p` 這種很短、單獨佔一行的）在複製貼上時消失了；建議複製 code
block 時用區塊右上角內建的「複製」按鈕，不要手動反白選取。**規避寫
法**：往後所有元件裡，只要標籤可能是 `<a>` 或別的標籤（例如「對方有沒有
建檔」這種條件式連結），一律用一個大寫變數（例如 `TargetTag` / `LeaderTag`
= `target ? "a" : "div"`）動態決定要渲染什麼標籤，而不是在檔案裡手打一
個單獨佔一行的 `<a`——這樣完全不會出現裸的 `<a` 文字，從根本上避開這個
複製貼上陷阱，`PersonOverview.astro`／`PersonRelations.astro`／
`[faction].astro` 都已採用這個寫法（無條件連結則退而求其次，把 `<a` 跟第
一個屬性合併寫在同一行，例如 `<a class="faction-person-slip"`）。

### 已知的踩坑模式：`getCollection()` 回傳型別推斷不出來，連鎖噴一整排錯

`[faction].astro`、`[id].astro` 都發生過：`getCollection("people")` 拿到
的資料在後續 `.map()`／`.filter()`／`.sort()` callback 裡型別被推成
`unknown`／`never`，導致 `entry`／`person`／`sourceLine` 這些參數被標成
implicit-any（`ts7006`），`frontmatter.xxx` 被標成「never 型別沒有這個屬
性」（`ts2339`），甚至把 `Map<unknown, unknown>` 傳給元件 props 時型別對不
上（`ts2322`）。**修法固定套路**：
1. 從 `astro:content` import 時多 import `type CollectionEntry`。
2. 需要用到的地方（`getStaticPaths` 參數、`interface Props` 裡的欄位、
   `.map()`／`.filter()` callback 參數）都明確標成 `CollectionEntry<"people">`
   或 `CollectionEntry<"people">[]`。
3. 頁面組件裡用 `Astro.props as Props` 取值。
4. 別漏掉巢狀 callback（例如 citations 陣列裡的 `(sourceLine: string) => {}`）
   ——這個最容易漏標，會在前面幾個都修完後才單獨冒出來。

## 一、人物頁資料格式（已定案，維持不變）

- 首頁：人物故事＋詳細資料為主、帶一點遊戲風格、勢力分類必留。
- 人物內容欄位：頭像、姓名、字號、簡介、籍貫、生卒年、諡號、所屬勢力與
  效力歷程、官職、親屬關係〔史實／後世分層〕、評價〔當世／後世〕、生平
  〔史實／演義分層〕、著作。
- **人物資料改用 Markdown + YAML frontmatter**，取代原本的 TypeScript 物
  件（`src/data/people/*.ts`）。最上面「填空區」放一次填好的基本資料，底
  下用固定的中文標題（`##`／`###`）＋段落／條列寫內容，史料出處直接寫書
  名、用「（來源：……）」註記，人物之間的連結用「（連結：目標人物代
  號）」，對方未建檔就整段不寫、之後補上會自動生效。
- `src/lib/parsePersonBody.ts`：不使用 Astro 內建的 Markdown 轉譯結果，直
  接讀 `entry.body` 原始文字自己解析固定中文標題／條列語法。

## 二、親屬關係卡片 UI 設計（已定案並實作完成）

- 一筆親屬記錄的畫面分三排：
  1. **人物名稱一排**：頭像＋姓名，對方已建檔就是可點連結＋真頭像，未建
     檔則不能點、頭像改用佔位圖（見下方「佔位頭像」）。名稱獨立一排，避
     免長名稱（例如「夏侯氏（名稱不詳）」）把下面的標籤擠壓變形。
  2. **標籤一排，左右分開放**：左邊是親屬分類標籤（父母／兄弟姊妹／配
     偶／子女／孫輩／其他，來自 `relationGroupLabel`，不管切史實還是後世
     都不會變）；右邊是資料性質標籤，只有哪一層有內容才顯示對應標籤。
  3. **內文一排**：顯示目前作用中那一層的說明文字。
- **右邊標籤定案為「史實」／「後世」（不是「演義」）**：現代作品併入後
  世文學，不再獨立拆出第三顆標籤；非史實那一層的說法不一定來自《三國演
  義》本身（可能是後世小說、遊戲等改編），標籤寫死「演義」會誤導讀者。
- **兩層都有內容時**：兩顆標籤都顯示、可點擊切換，預設先顯示「史實」；
  只有一層有內容時，該標籤顯示成不能點的純標示（不需要切換互動）。
- **實作方式**：`PersonRelations.astro` 用 `data-relation-card` /
  `data-relation-layer-btn` / `data-relation-layer` 這組 data attribute，
  搭配檔案底部一段 `<script>` 做點擊切換（跟 `[id].astro` 主頁籤、
  `[faction].astro` 搜尋框同一套「data attribute + 原生 JS」模式，全站風
  格一致，不用額外框架）。
- **已知簡化、之後可能要擴充**：「後世」標籤底下如果同時有演義、現代遊戲
  等不同來源，目前 `parsePersonBody.ts` 的 `ParsedRelation.romance` 還是
  單一字串欄位，這種子分類（「演義：……」「現代遊戲：……」）目前**由撰稿
  人自己在同一段文字裡手動寫清楚**，解析器沒有幫忙拆成結構化欄位。等實
  際填寫劉備／關羽這類真的有多重後世來源的人物、發現手動格式不夠用時，
  再回頭擴充 `parseRelations()` 的解析規則。

### 佔位頭像（已定案並實作完成）

宇軒提供一份 `_placeholder.svg`（沿用舊專案素材），**取代掉原本「姓氏第
一個字＋圓圈」的文字佔位做法**，套用在所有「對方沒有自己人物頁」的頭像位
置——親屬卡片、效力歷程的君主／效力對象都算。做法：存到
`public/images/_placeholder.svg`，程式碼一律直接渲染 `<img>`（不再判斷要
不要顯示文字圓圈），沒有真頭像時 `src` 就指向這個固定路徑
`${base}images/_placeholder.svg`。`PersonOverview.astro`（效力歷程）與
`PersonRelations.astro`（親屬）都已改用這個邏輯，`character-detail.css`
裡對應的文字佔位樣式（`.person-service-leader__placeholder` 等）已移除。
**待確認**：宇軒尚未回報是否已實際把 `_placeholder.svg` 放進
`public/images/` 資料夾——這是他自己要放的檔案，不是娜娜這邊能生成／傳送
的。

### 史實／演義視覺色彩區分（本輪新定案）

宇軒回報生平時間軸最後一筆／唯一一筆不會顯示左側連接槓槓，且提出「要不要
把史實生平（紅）跟演義生平（綠）做顏色區分、親屬卡片的史實／後世標籤是不
是也要跟著統一」的設計提問，娜娜給出的判斷、宇軒已採用：

- **時間軸連接槓槓一律顯示**：拿掉
  `.person-biography-entry:last-child .person-biography-entry__track::before
  { display: none; }` 這條規則，不管是不是最後一筆／唯一一筆都顯示點＋槓
  槓，視覺一致。
- **新增顏色變數 `--person-romance: #3f6e63`**（低彩度墨綠），跟既有
  `--person-cinnabar`（朱紅，史實／引用色）配對成一組「史實＝紅、後世＝
  綠」的顏色邏輯；選這個墨綠色是為了跟蜀漢勢力既有的橄欖綠
  `--person-tone: #68745c` 區隔開，不會混在一起分不清。
- **套用範圍**：
  - `PersonBiography.astro` 的 `<ol>` 新增 `isRomance` 時的
    `person-biography-list--romance` 修飾 class，時間軸上的圓點
    （`.person-biography-entry__track span`）在演義生平時變成墨綠色，連接
    線本身維持中性色 `--person-line` 不變（只有點變色，線不變，避免整條
    軸線太花）。
  - `PersonRelations.astro` 的史實／後世標籤（不管是可切換的 `<button>`
    還是唯一一層時的 `<span>`）都套用對應色系：史實＝朱紅系、後世＝墨綠
    系，未選中／唯讀時是淡色調（idle-tint），選中／啟用時是實色填滿
    （`.is-active`）。
  - **刻意不變色的例外**：生平段落裡「與史實的差異」提示框
    （`.person-biography-entry__difference`）維持朱紅色，不跟著演義的墨
    綠色走——這是娜娜的設計判斷，理由是這個框本質是「提醒讀者注意落差」
    的警示色，不是「這段內容屬於史實」的分類色，兩者語意不同。**這條是
    娜娜主動提出、宇軒尚未明確表態是否認同，之後如果宇軒覺得怪可以再
    調整。**

## 三、目前「從零重建」進度（Task List 狀態，供接手者對照）

1. ✅ 清空本機專案資料夾、重新 `npm create astro@latest`。
2. ✅ `astro.config.mjs`（含 `markdown: { processor: unified() }`）。
3. ✅ `src/content.config.ts`（`people` content collection + zod schema，
   已加中文註解）。
4. ✅ `src/lib/parsePersonBody.ts`。
5. ✅ 人物詳細頁元件與樣式，全部交付完成：
   - `src/data/factions.ts`、`src/types/person.ts`（精簡版）。
   - `src/components/person/PersonHero.astro`。
   - `src/components/SiteHeader.astro` ＋ `src/styles/site-header.css`
     （曾因複製貼上漏掉 `<a` 卡關，已修好）。
   - `src/styles/character-detail.css`（主樣式；親屬關係、佔位頭像、時間
     軸連接槓槓、史實／演義配色都已套用本輪最新設計，不是最初交付的舊版
     樣式）。
   - `src/components/person/PersonBiography.astro`（含 `isRomance` 配色）、
     `PersonEvaluations.astro`、`PersonWorks.astro`、
     `PersonOverview.astro`（效力歷程頭像已改用佔位圖邏輯、已拿掉多餘的
     「諡號補充」小標題，只留「身後追諡」眉標）、`PersonRelations.astro`
     （依第二節新設計實作，含史實／後世標籤切換、佔位頭像、色彩區分）。
6. ✅ `src/pages/character/[id].astro`（頁面組裝：`getStaticPaths`、
   `parsePersonBody`、`personLookup`、分頁 tabs 切換 script，已修好
   `getCollection` 型別推斷問題），放回 `guan-yu.md` ＋ `guan_yu.png`，第
   一個人物頁（關羽）已在瀏覽器實際跑起來並確認畫面正常，六個分頁都能正
   常渲染。
7. ✅ `src/pages/faction/[faction].astro` ＋ `src/styles/
   faction-overview.css`（勢力總覽頁，含搜尋過濾），宇軒已確認畫面正常。
   `zhang-fei.md`／`guan-ping.md` 也已放回 `src/content/people/`，但兩人
   的頭像圖檔 `zhang_fei.png`／`guan_ping.png` 目前沙盒裡沒有（舊 repo
   GitHub 同步規則排除了 `/public/`），**需要宇軒自己提供**。
8. ⏳ 下一步（待宇軒選擇先做哪個）：
   - `src/pages/index.astro`（首頁）——尚未開始。
   - `liu-bei.md`（劉備人物資料）——尚未開始撰寫。

## 四、重要踩坑記錄：Astro 7 的 Sätteri Markdown 引擎在 Windows 上裝不上

宇軒在公司 Windows 電腦（不能裝額外系統元件、不能動 Node 版本）上跑
`npm run dev` 時，卡在 `[content] Syncing content` 這一步，報錯是找不到
`@bruits/satteri-win32-x64-msvc` 的原生 `.node` 檔案。

**根本原因：** Astro 6.4 引進、Astro 7 起預設使用 Sätteri（Rust 寫的原生
Markdown 處理器）處理 content collections；舊版人物資料是純 TypeScript 物
件，從來沒有觸發過任何 Markdown 處理流程，這次改用 `.md` 檔案才第一次真
正呼叫到這顆原生引擎。Astro 啟動時會把整個 collection 掃過一輪、預先跑一
次 Markdown 處理，不管頁面程式碼有沒有真的呼叫轉譯結果都會執行。

**解法（已驗證有效，已內建在「從零重建」流程第 2 步）：** 在
`astro.config.mjs` 加上 `markdown: { processor: unified() }`（`import
{ unified } from "@astrojs/markdown-remark"`），並安裝
`@astrojs/markdown-remark`，換回純 JavaScript、沒有原生依賴的
Unified／Remark 處理器。因為人物頁是自己讀 `entry.body` 原始文字解析，不
吃 Astro 內建轉譯結果，換引擎對畫面顯示零影響。

## 五、內容落差盤點（對照舊版 Three-Kingdoms-Biographies-Astro）

- **必須補回、待宇軒／奈奈確認設計後才動工**：三國文獻庫（獨立網站）的引
  用連結功能——舊版靠 `sourceId` → `sources.json` 的 `defaultTextUnitId`
  或單筆 citation 的 `textUnitId`，對到 `text-units.json` 產生可點擊連
  結。新版目前的引用只是純文字「（來源：……）」，沒有連結機制。**娜娜提
  出的方案（尚未實作，等確認）**：新增 `src/data/referenceLinks.ts` 存放
  `{ source, locator?, url }` 清單，人物 `.md` 完全不用改寫法、不用加任何
  ID；畫面渲染時自動比對現有「（來源：書名・卷次）」文字，比對到就包成
  連結，比對不到維持純文字（不會報錯或整段消失）。
- **可以放心捨棄**：`published`／`draft` 發布旗標（改用 git commit／push
  當作發布關卡）、`filterGroup`（新版 faction + factionStages 已涵蓋）。
- **待宇軒自己判斷**：`otherNames` 要不要比照舊版拆成 `otherNames`／
  `commonAlias`（後世通稱）兩種，涉及既有的「保守命名原則」，還沒決定。

## 六、待辦／下一步

- 等宇軒確認本輪交付的三個檔案（`character-detail.css`／
  `PersonBiography.astro`／`PersonRelations.astro`：時間軸連接槓槓一律顯
  示、史實紅／後世綠配色）貼上後畫面正常，才繼續下一步。
- 宇軒需自行提供：`zhang_fei.png`、`guan_ping.png` 頭像圖檔；確認
  `_placeholder.svg` 是否已放進 `public/images/`。
- 下一步二選一（待宇軒指示）：`src/pages/index.astro`（首頁）或
  `liu-bei.md`（劉備資料）。
- 文獻庫引用連結機制：等宇軒確認第五節方案後動工。
- 程式碼交付方式已全面改成逐步貼檔案內容確認，**不再打包 zip 整包覆
  蓋**（見第零節），但視改動複雜度可彈性改用「整份檔案貼上覆蓋」取代局部
  diff。沙盒 clone 僅本機 commit、未 push（宇軒未提供 push 權限），宇軒之
  後要正式上 GitHub 需自己手動 push（習慣用 GitHub Desktop）。
- 其餘人物（劉備、張飛、關平等）依新格式補檔；張飛／關平內容已放回但缺頭
  像圖檔，劉備尚未撰寫。
- `parseRelations()` 的「後世」子分類（演義／現代遊戲…）目前靠撰稿人手動
  格式化，還沒有結構化欄位，等實際使用發現不夠用再擴充（見第二節）。
- `parsePersonBody.ts` 只驗證過關羽＋張飛／關平幾份資料的寫法，還沒有大
  量不同人物、不同邊界情況的壓力測試。
- 「與史實的差異」提示框維持朱紅、不跟著演義綠色走——娜娜的設計判斷，宇
  軒尚未明確表態是否認同，可再調整（見第二節）。
- 「網頁元件怎麼拆」這個討論尚未細談。
- `otherNames`／`commonAlias` 拆分決策待宇軒定案。
- 舊版 Astro 專案（`Three-Kingdoms-Biographies-Astro`）與文獻庫
  （`Three-Kingdoms-Reference-Texts`）維持獨立不動，不併進來討論。